package L23.CompAndOpt;

import java.util.Comparator;

public class Comp {
	// comp with lambda used in Mannschaft
	//.max(Comparator.comparing(i -> i.gegentore))

	// Optinal with ifPresent was already used in L19.Browser
}
